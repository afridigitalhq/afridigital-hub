const persona=require('../persona');const memory=require('../memory-v2');function route(input){const msg=(input.message||'').toLowerCase();const p=persona.getPersona(input.userId||'u');if(msg.includes('error')||msg.includes('fix')||p.tone==='precise'){return 'reasoning';}if(msg.length<15){return 'fast';}if(msg.includes('remember')||msg.includes('what did i say')){return 'memory';}return 'balanced';}module.exports={route};
