const intel=require('../system-intel');const router=require('../swapper-v3');const engines=require('../swapper-v3/engines');function adjust(engine,system){if(system.modules.includes('memory')&&engine==='memory')return 1.2;if(system.modules.length<5)return 0.8;return 1;}module.exports={route:(input)=>{const sys=intel.get();const base=router.route(input);return base},system:sys=>intel.snapshot()};
