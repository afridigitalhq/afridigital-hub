services:
  - type: web
    name: afridigital-hub
    env: node
    rootDir: .
    buildCommand: cd SaaS/afridigital-backend && npm install && cd ../afridigital-frontend && npm install && npm run build && cd ../afridigital-backend
    startCommand: node server.js
