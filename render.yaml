services:
  - type: web
    name: afridigital-backend
    env: node

    rootDir: SaaS/afridigital-backend

    buildCommand: |
      npm install
      npm run build --prefix ../afridigital-frontend

    startCommand: node server.js
