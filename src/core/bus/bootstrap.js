import { AfriBus } from "./AfriKernelEventBus";

// AFRIKERNEL_AUTHORITY_LOCK_V2 (EVENT LOG → DAGRuntime → RENDER ONLY)

export const AFRIKERNEL_MODE = "DAG_ONLY";

export const AFRI_KERNEL_BOOTSTRAP = {
  mode: AFRIKERNEL_MODE
};
