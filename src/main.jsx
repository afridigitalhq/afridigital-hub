import React from "react";
import ReactDOM from "react-dom/client";
import ControlPlaneUI from "./control-plane/ControlPlaneUI";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ControlPlaneUI />
  </React.StrictMode>
);
