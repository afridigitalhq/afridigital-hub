import React from "react";
import ReactDOM from "react-dom/client";
import SOCMainEntry from "./soc-os/entry/SOCMainEntry.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SOCMainEntry />
  </React.StrictMode>
);
